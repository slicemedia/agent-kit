# Changesets

The pre-launch changesets were consolidated into the initial `0.1.0` entry in `CHANGELOG.md`.

Add a new changeset for every user-visible change after `0.1.0`. Choose the smallest semantic
version increment that accurately describes the compatibility impact; do not edit an already
published release entry to describe later work.
