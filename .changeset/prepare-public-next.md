---
"@slicemedia/agent-kit": patch
---

Test Agent Kit on Windows and Linux with Node 22.13+ and 24, document platform-specific profile privacy guarantees, add private runtime-configured plaintext and encoded release-denylist scanning, and stage a fail-closed npm `next` workflow that separates unprivileged preparation and verification from the minimal OIDC publisher of one exact approved main-commit archive.
